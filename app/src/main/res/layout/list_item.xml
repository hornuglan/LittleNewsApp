<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/pillar_name"
        style="@style/MainText"
        tools:text="@string/list_item_section_placeholder" />

    <TextView
        android:id="@+id/date"
        style="@style/MainText"
        tools:text="@string/list_item_date_placeholder" />

    <TextView
        android:id="@+id/author"
        style="@style/MainText"
        android:text="@string/list_item_author_placeholder"/>

    <TextView
        android:id="@+id/title"
        style="@style/TitleText"
        tools:text="@string/list_item_title_placeholder" />

</LinearLayout>